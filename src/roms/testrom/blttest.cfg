MEMORY {
    ROM: 		    start = $8000, size = $4000, file = %O, fill=no;
}
SEGMENTS {
    CODE_ROMHEADER:     load = ROM, type = ro;
    CODE:     	        load = ROM, type = ro;
    RODATA:	            load = ROM, type = ro;
}
FEATURES {
}
SYMBOLS {
}